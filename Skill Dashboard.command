#!/usr/bin/env bash
# Double-click this file in Finder to open the skill-usage dashboard.
# It starts a tiny local server and opens your browser; close this window to stop.
cd "$(dirname "$0")"
exec ./bin/skill-dashboard
