#!/usr/bin/env bash
# Migrate Things/Notion task items into issue trackers.
set -euo pipefail

here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
script="$here/../skills/work-migration/scripts/work-migrate.mjs"

exec node "$script" "$@"
